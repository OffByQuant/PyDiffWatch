# PyDiffWatch against llama-swap on another machine (e.g. a GPU box on the LAN).
# llama-swap loads a model on its first request; probe_timeout covers that wait.
# `model` must be one of the ids at http://<host>:8000/v1/models.

[reviewer]
provider = "openai"
base_url = "http://192.168.1.50:8000/v1"
model = "gemma-singleshot"
structured_output = "json_schema"
timeout = 300.0
# malicious_min_confidence = 0.8   # a weaker malicious verdict alerts as suspicious and waits for manual review

[reviewer.extra_body]
chat_template_kwargs = { enable_thinking = false }
